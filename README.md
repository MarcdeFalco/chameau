# chameau

**Chameau** a Caml Light Light interpreter
------------------------------------------

Compilation
===========
* To install `menhir` : `opam install menhir`
* Then `dune build`

Dependancies
============
* ocaml
* menhir
* ocamlbuild
